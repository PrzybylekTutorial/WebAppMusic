// Top-level build file where you can add configuration options common to all sub-projects/modules.

plugins {
    // Versions are defined in each module plugin block to keep this file minimal
}

allprojects {
    // You can add common configuration here if needed
}